<div class="img"><img src="/images/plLogo.png" class="logoBan bann rounded mx-auto d-block" alt="image"></div>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="{{path('homepage')}}">PremLeague</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
             </button>

            <div class="collapse navbar-collapse" id="navbarColor03">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('homepage')}}">Accueil</a>
                     </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('calpage')}}">Calendrier</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('equipespage')}}">Equipes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('classpage')}}">Classement</a>
                    </li>
                </ul>
                {% if app.user %}
                    <ul class="navbar-nav">
                        <li class="nav-item">
                                <a class="nav-link" href="{{path('account_logout')}}">Déconnexion</a>
                        </li>
                    </ul>
                {% endif %}
            </div>
        </nav>

        {% for label, messages in app.flashes %}
            <div class="container">
                <div class="alert alert-{{label}}">
                    {% for message in messages %}
                        <p>{{message | raw}}</p>
                    {% endfor %}
                </div>
            </div>
        {% endfor %}